import { ButtonAddPipe } from './button-add.pipe';

describe('ButtonAddPipe', () => {
  it('create an instance', () => {
    const pipe = new ButtonAddPipe();
    expect(pipe).toBeTruthy();
  });
});
